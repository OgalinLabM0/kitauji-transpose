export const COMMA_LOCATION_PROMPT = `程序已确认中文草稿比日文少一个逗号，其他标点的种类与顺序一致。
对照日文停顿和中文语法，找一个可以插入逗号的位置，不翻译、不改写任何字。
只输出JSON：{"prefix":"从中文草稿开头到插入点之前的全部原样文字"}。程序在prefix后插入，后面的草稿原样保留。
prefix不能是整篇草稿，不能以标点或空格结尾，插入点后面也不能紧接标点或空格。
无法只加一个逗号而保持中文自然时，输出{"prefix":null}。不输出其他字段或解释。`;
